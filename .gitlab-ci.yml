image: ruby:2.4
pages:
  stage: deploy
  script:
  - apt install jekyll
  - gem install jekyll bundle
  - bundle
  - bundle install
  - bundle exec jekyll build -d public
  artifacts:
    paths:
    - public
  only:
  - master
